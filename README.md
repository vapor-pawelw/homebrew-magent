# Deprecated tap

This tap is deprecated.

The `magent` cask has moved to `vapor-pawelw/tap`.

Existing installs should migrate automatically after `brew update`.
New installs should use:

```bash
brew tap vapor-pawelw/tap
```
